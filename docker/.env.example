# `openssl rand -hex 32` などで生成し、docker/.env にだけ保存する。
# 初回セットアップ完了後は空にしてコンテナを再作成する。
ILLUMIA_SETUP_TOKEN=

# Pangolin 経由の HTTPS では true のままにする。
ILLUMIA_SECURE_COOKIES=true

# proxy が client supplied header を除去・再設定し、origin port が閉じている場合のみ true。
ILLUMIA_TRUST_PROXY_HEADERS=false
